/**
 * 配置文件模板
 */

export default {
  // `baseURL` 将自动加在 `url` 前面，除非 `url` 是一个绝对 URL。
  //baseURL: 'http://syl.com/api',
   baseURL: 'http://182.254.185.182/api',

  //图片请求地址前缀, 除非 `url` 是一个绝对 URL。
  //baseImgURL: 'http://syl.com/uploads/',
   baseImgURL: 'http://182.254.185.182/uploads/',
}
